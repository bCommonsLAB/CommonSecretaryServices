**# Grundlegende Prinzipien**
[Bestehende Prinzipien bleiben...]

**# Dataclass & Typen**
- Verwende native Python Dataclasses statt Pydantic
- Implementiere strikte Typ-Annotationen mit dem typing Modul
- Validiere Daten in __post_init__ Methoden
- Nutze frozen=True für unveränderliche Datenstrukturen
- Implementiere to_dict() und from_dict() Methoden
- Verwende TypeVar für generische Typen
- Füge mypy Typ-Checks hinzu

**# API Response Struktur**
- Folge dem standardisierten Response-Format:
  - status (success/error)
  - request (Kontext)
  - process (Verarbeitungsdetails)
  - data (Ergebnisdaten)
- Tracke LLM-Nutzung in process.llm_info
- Gruppiere Daten logisch in input/output
- Verwende Millisekunden für Zeitangaben
- Dokumentiere Fehler mit code/message/details

**# Fehlerbehebung**
[Bestehende Prinzipien bleiben...]

**# Entwicklungsprozess**
[Bestehende Prinzipien bleiben...]
- Validiere neue Dataclasses mit mypy
- Teste Response-Strukturen auf Konformität
- Prüfe LLM-Tracking Vollständigkeit

**# Kommentare**
[Bestehende Prinzipien bleiben...]
- Dokumentiere Validierungslogik in __post_init__
- Erkläre komplexe Typ-Annotationen
- Beschreibe Response-Strukturen und deren Zweck

**# Code-Struktur**
- Basis-Dataclasses in core/models.py
- Validierungs-Utilities in core/validation.py
- Response-Modelle in api/responses.py
- Prozessor-spezifische Modelle in processors/[name]/models.py

**# Validierung**
- alle Variablen müssen mit expliziten Typ-Annotationen versehen werden
- Implementiere Validierungs-Dekoratoren für häufige Checks
- Nutze __post_init__ für komplexe Validierung
- Stelle sicher, dass alle Responses dem Standard folgen
- Prüfe Typ-Korrektheit mit mypy

**# Performance**
- Nutze slots=True für optimierte Dataclasses
- Implementiere caching wo sinnvoll
- Minimiere Verschachtelung in Responses
- Optimiere Serialisierung/Deserialisierung

**# Testing**
- Teste Dataclass-Validierung
- Prüfe Response-Konformität
- Validiere LLM-Tracking
- Teste Fehlerszenarien
alle Testdateien unter .tests/ organisieren

**#Serverstart**
Immer in virtueller Pythonumgebung starten:
venv\Scripts\activate; $env:PYTHONPATH = "."; python src/main.py